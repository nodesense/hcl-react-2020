import React from 'react';

export const tamil = {
    contact_label: 'தொடர்பு'
}

export const english = {
    contact_label: 'Contact Us'
}

// default value
const LanguageContext = React.createContext(tamil);
export default LanguageContext;